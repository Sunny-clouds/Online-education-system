<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.onik.eduspring.mapper.ScoreMapper">
    <!--根据id修改成绩信息-->
    <update id="setScore">
        update score
        <set>
            <if test="usualScore != null">usual_score = #{usualScore},</if>
            <if test="totalScore != null">total_score = #{totalScore},</if>
            <if test="gradeLevel != null">grade_level = #{gradeLevel},</if>
            <if test="remark != null">remark = #{remark}</if>
        </set>
        where id = #{id}
    </update>

    <!--根据学生姓名模糊查询成绩-->
    <select id="getScoreByUserName" resultType="com.onik.eduspring.vo.ScoreVo">
        SELECT stu.nickname AS userName,c.title,s.exam_name,s.usual_score,s.exam_score,s.total_score,s.grade_level,tea.nickname AS teacherName,s.remark
        FROM score s
        JOIN user stu ON s.student_id = stu.id
        JOIN user tea ON s.teacher_id = tea.id
        JOIN course c ON s.course_id = c.id
        WHERE stu.nickname LIKE CONCAT('%', #{username}, '%')
    </select>

    <!--查询所有成绩-->
    <select id="getAll" resultType="com.onik.eduspring.vo.ScoreVo">
        SELECT stu.nickname userName,c.title,s.exam_name,s.usual_score,s.exam_score,s.total_score,s.grade_level,tea.nickname teacherName,s.remark
        FROM score s
        JOIN user stu ON s.student_id = stu.id
        JOIN user tea ON s.teacher_id = tea.id
        JOIN course c ON s.course_id = c.id
        where s.teacher_id = #{userId}
        order by s.total_score desc
    </select>

    <!--根据课程模糊查询成绩-->
    <select id="getScoreByTitle" resultType="com.onik.eduspring.vo.ScoreVo">
        SELECT stu.nickname AS userName,c.title,s.exam_name,s.usual_score,s.exam_score,s.total_score,s.grade_level,tea.nickname AS teacherName,s.remark
        FROM score s
        JOIN user stu ON s.student_id = stu.id
        JOIN user tea ON s.teacher_id = tea.id
        JOIN course c ON s.course_id = c.id
        where c.title LIKE CONCAT('%', #{title}, '%')
        order by s.total_score desc
    </select>
</mapper>