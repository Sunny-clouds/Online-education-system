<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.onik.eduspring.mapper.QuestionMapper">

    <!--添加题目-->
    <insert id="saveExamTitle" parameterType="com.onik.eduspring.entity.Question">
        insert into question(type,course_id,content,option_a,option_b,option_c,option_d,
                             correct_answer,analysis,score,difficulty,create_user,create_time)
        values(#{type},#{courseId},#{content},#{optionA},#{optionB},#{optionC},#{optionD},
               #{correctAnswer},#{analysis},#{score},#{difficulty},#{createUser},#{createTime})
    </insert>

    <!--修改题目-->
    <update id="updateExamTitle" parameterType="com.onik.eduspring.entity.Question">
        update question
        <set>
            <if test="type != null">type = #{type},</if>
            <if test="courseId != null">course_id = #{courseId},</if>
            <if test="content != null">content = #{content},</if>
            <if test="optionA != null">option_a = #{optionA},</if>
            <if test="optionB != null">option_b = #{optionB},</if>
            <if test="optionC != null">option_c = #{optionC},</if>
            <if test="optionD != null">option_d = #{optionD},</if>
            <if test="correctAnswer != null">correct_answer = #{correctAnswer},</if>
            <if test="analysis != null">analysis = #{analysis},</if>
            <if test="score != null">score = #{score},</if>
            <if test="difficulty != null">difficulty = #{difficulty},</if>
            <if test="createUser != null">create_user = #{createUser},</if>
            <if test="updateTime != null">update_time = #{updateTime}</if>
        </set>
        where id = #{id}
    </update>

    <!--删除题目-->
    <delete id="delExamTitle" parameterType="java.lang.Long">
        delete from question where id = #{id}
    </delete>

    <!--获取题库所有题目信息-->
    <select id="getAllExamTitle" resultType="com.onik.eduspring.vo.QuestionVo">
        select q.id,q.type,c.title,q.content,q.option_a,q.option_b,
               q.option_c,q.option_d,q.correct_answer,q.analysis,
               q.score,q.difficulty,u.nickname,q.create_time,q.update_time
        from question q
        left join user u on q.create_user = u.id
        left join course c on q.course_id = c.id
    </select>

    <!--获取指定题型题目-->
    <select id="selectByType" resultType="com.onik.eduspring.entity.Question">
        select * from question where type = #{type} and course_id = #{courseId}
    </select>

    <!--根据题目id获取正确答案-->
    <select id="getExamTitleAnswerById" resultType="com.onik.eduspring.dto.StudentAnswerDto" parameterType="java.util.List">
        select id as question_id,correct_answer,score from question
        where id in
        <foreach item="id" collection="list" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

</mapper>